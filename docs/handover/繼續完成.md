# 築未科技 — 繼續完成

**用途**：接續「使用 MCP 完成三件事」後，已執行項目與你仍需手動完成的項目。

---

## 一、已繼續完成（本次）

| 項目 | 結果 |
|------|------|
| **Tunnel 日誌** | 已查看；錯誤為「Provided Tunnel token is not valid」→ .env 的 CLOUDFLARE_TOKEN 須為**完整 Token**（從 Install connector 複製），不是 tunnel ID（UUID）。 |
| **Tunnel 容器** | 已執行 `docker compose stop tunnel`，避免持續重啟。 |
| **部屬到外網.md** | 已補充：CLOUDFLARE_TOKEN 須為完整 Token（eyJ... 開頭），不是隧道 ID。 |
| **使用MCP完成三件事.md** | 已補充：tunnel 若 Restarting，須更新 .env 為完整 Token 後 `docker compose down` 再 `docker compose up -d`。 |
| **D 槽同步** | 已執行 `python scripts/deploy_to_d_drive.py`，D:\brain_workspace 已同步。 |
| **test_brain_server** | 已執行，全部通過（/health、/、/admin、/chat）。 |
| **git 提交** | 已提交：git_push.bat、使用MCP完成三件事.md、部屬到外網.md。 |

---

## 二、你仍需手動完成

| 項目 | 你要做的 |
|------|----------|
| **git push** | 執行：`scripts\git_push.bat https://github.com/你的帳號/你的repo.git`（URL 換成你的）。 |
| **完整跑八階段** | 在 .env 填入 `GEMINI_API_KEY=你的key`；本機啟動 Ollama；之後用 **啟動完整跑.bat** 或 `python scripts/run_full_system.py`。 |
| **外網 brain.zhe-wei.net** | 在 .env 將 `CLOUDFLARE_TOKEN` 改為**完整 Token**（從 Zero Trust → Tunnels → 你的隧道 → **Install connector** 複製整段），儲存後執行 `docker compose up -d`（tunnel 會再啟動）。 |

---

## 三、目前狀態

- **brain_server**：Docker 容器 zhewei_brain 在跑，端口 8000 可用；管理介面 /admin、手機頁 /chat 可開。
- **tunnel**：已停止（Token 無效）；更新 .env 後執行 `docker compose up -d` 即可再啟用。
- **Railway**：https://zhewei-ai-chat-01-production.up.railway.app 已對外，可直接用。

---

*繼續完成 — 供接續開發或交接時對照。*
