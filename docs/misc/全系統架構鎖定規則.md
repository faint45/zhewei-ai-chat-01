# 築未科技 - 全系統架構鎖定規則 (2026-02-05)

## 1. 核心檔案保護

- 嚴禁修改 `agent_tools.py` **(213–234 行)** 的 `run_vision_engine` 跨環境呼叫邏輯。
- 嚴禁更動 `report_generator.py` 中**影像報表 (CSV/JSONL)** 與 **語音報表 (Markdown)** 的分流路徑。

## 2. 儲存架構分流

- **熱資料 (D 槽 808GB)**：僅存放 `input/`、`processed/`、`models/` 與 `venv`。
- **溫資料 (外接 5.1TB)**：存放原始影片 (`raw_footage/`)。
- **冷資料 (雲端 4.1TB Z 槽)**：存放 `Reports/` 與長期記憶。

## 3. 代碼更新規範

- 所有程式碼更新必須提供「**修正後的完整檔案**」，絕不接受局部修改區段。
- 進度數值必須使用 **LaTeX 格式**呈現，例如：`$100\%$`。
