# 築未科技 - 架構守則 (2026-02-05 修訂)

## 1. 核心結構禁止修改

- 嚴禁變動 **D:/brain_workspace** 與 **Z:/Zhewei_Brain** 的分流架構。
- 嚴禁修改 **3.14（主腦）** 與 **3.12（視覺）** 的環境隔離邏輯。
- 所有代碼更新必須為「**完整覆蓋**」，嚴禁提供「修改區段」。

## 2. 數據格式規範

- 報表進度必須使用 **LaTeX 格式**（如 `$85\%$`）。
- 日誌與報表必須**同步存入 Z 槽**，不得僅存於本地。

## 3. 工具調度

- 所有工具必須定義在 `agent_tools.py` 的 **`TOOLS`** 字典中。
