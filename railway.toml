# Railway部署配置
port = 8006

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -c \"import uvicorn; uvicorn.run('seven_stage_api:app', host='0.0.0.0', port=int(__import__('os').getenv('PORT', 8006)))\""
