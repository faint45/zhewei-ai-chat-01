# MCP 第一波工具測試清單

## 📝 測試前準備

### 1. 重新載入 Cursor MCP 設定
```
Cursor → Settings (Ctrl+,) → Features → Model Context Protocol → Reload
```
或直接重啟 Cursor

### 2. 確認工具已載入
在 Cursor Composer 中，輸入 `@` 應該會看到新增的工具：
- `@docker-mcp`
- `@git`
- `@fetch`

---

## ✅ 測試項目

### Test 1: Docker MCP
**在 Cursor Composer 中輸入：**
```
用 docker-mcp 列出所有容器
```

**預期結果：**
- 應該列出 `zhewei_brain`, `zhe-wei-tech-tunnel-1`, `zhe-wei-ollama`, `zhewei-qdrant` 等容器
- 顯示每個容器的狀態（running/exited）

**如果失敗：**
- 確認 Docker Desktop 正在執行
- 檢查 `docker ps -a` 在終端機能否執行

---

### Test 2: Git MCP
**在 Cursor Composer 中輸入：**
```
用 git 查看目前分支狀態
```

**預期結果：**
- 顯示目前分支名稱（例如：`main` 或 `master`）
- 列出未提交的變更（如果有）
- 顯示最近的 commit

**如果失敗：**
- 確認 `D:/zhe-wei-tech` 是 Git 儲存庫
- 檢查 `.cursor/mcp.json` 中 git 路徑是否正確

---

### Test 3: Fetch MCP
**在 Cursor Composer 中輸入：**
```
用 fetch GET http://127.0.0.1:8002/health
```

**預期結果：**
- 回傳 JSON 格式的健康檢查資訊
- 包含 `status`, `ollama`, `voice` 等欄位

**如果失敗：**
- 確認 `brain_server.py` 正在執行（port 8002）
- 或改測試：`用 fetch GET https://api.github.com/zen`

---

## 🔍 進階測試

### Test 4: Docker 日誌查看
```
用 docker-mcp 查看 zhewei_brain 容器的最後 50 行日誌
```

### Test 5: Git 歷史查詢
```
用 git 顯示最近 5 次 commit 的摘要
```

### Test 6: Fetch POST 請求
```
用 fetch POST http://127.0.0.1:8002/api/health/summary
```

### Test 7: Docker 容器重啟
```
用 docker-mcp 重啟 zhe-wei-ollama 容器
```

---

## 🐛 常見問題排查

### 問題 1: MCP 工具沒有出現
**解決方法：**
1. 檢查 `.cursor/mcp.json` 格式是否正確（JSON 語法）
2. 確認沒有多餘的逗號或缺少逗號
3. 完全關閉 Cursor 後重新開啟
4. 查看 Cursor 的 MCP 日誌（Settings → MCP → View Logs）

### 問題 2: docker-mcp 無法連接
**解決方法：**
```bash
# 在終端機測試
docker info

# 如果失敗，啟動 Docker Desktop
```

### 問題 3: git 工具找不到儲存庫
**解決方法：**
檢查 `.cursor/mcp.json` 中的路徑：
```json
"git": {
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-git", "D:/zhe-wei-tech"],
  "env": {}
}
```
確認 `D:/zhe-wei-tech` 是正確的絕對路徑

### 問題 4: fetch 連線失敗
**解決方法：**
```bash
# 在終端機測試
curl http://127.0.0.1:8002/health

# 或
python -c "import urllib.request; print(urllib.request.urlopen('http://127.0.0.1:8002/health').read())"
```

---

## 📊 測試結果記錄

| 測試項目 | 狀態 | 備註 |
|---------|------|------|
| docker-mcp 列出容器 | ⬜ 未測試 |  |
| git 查看分支 | ⬜ 未測試 |  |
| fetch GET 請求 | ⬜ 未測試 |  |
| docker-mcp 查看日誌 | ⬜ 未測試 |  |
| git 查看歷史 | ⬜ 未測試 |  |
| fetch POST 請求 | ⬜ 未測試 |  |

---

## 🎯 測試完成後

如果所有測試通過，可以：
1. 更新 `MCP_擴充上線說明.md`，加入第一波工具
2. 準備安裝第二波工具（postgres, slack, everything）
3. 開始使用新工具改善日常工作流程

如果有測試失敗，請記錄錯誤訊息並檢查上方的排查步驟。
